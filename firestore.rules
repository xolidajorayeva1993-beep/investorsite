rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Barcha client-side kirish taqiqlangan.
    // Faqat firebase-admin SDK (server) orqali kirish mumkin.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
